// Keep static images as Metro-managed assets. Embedding base64 image data in
// TypeScript makes every parse, type check, and bundle unnecessarily larger.
export const SRV_LOGO = require('../../assets/srv-login-logo.png');
